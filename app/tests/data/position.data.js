import { positions } from './data';

class PositionData {
    getPosition() {
        return Object.values(positions)[0];
    }

    getPositions() {
        return projects;
    }
}

export default PositionData